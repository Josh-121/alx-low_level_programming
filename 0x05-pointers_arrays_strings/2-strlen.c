#include <string.h>
#include "main.h"
/**
 * _strlen -  function that returns the length of a string
 *
 * @s: char type pointer
 *
 * Return: string length of a string
 */
int _strlen(char *s)
{
int len = strlen(s);
return (len);
}
